import { Username.Validators } from './username.validators';

describe('Username.Validators', () => {
  it('should create an instance', () => {
    expect(new Username.Validators()).toBeTruthy();
  });
});
