import { SearchInputDirective } from './search-input.directive';

describe('SearchInputDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchInputDirective();
    expect(directive).toBeTruthy();
  });
});
